export default function VerifyPage() {
  return (
    <div
      style={{
        color: "#1A1A1A",
        padding: "4rem 2rem",
        fontFamily: "var(--font-display)",
        fontSize: "2rem",
      }}
    >
      Verify OTP
    </div>
  );
}
