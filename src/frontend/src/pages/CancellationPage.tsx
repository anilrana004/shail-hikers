export default function CancellationPage() {
  return (
    <div
      style={{
        color: "#FAD4D8",
        padding: "4rem 2rem",
        fontFamily: "var(--font-display)",
        fontSize: "2rem",
      }}
    >
      Cancellation Policy
    </div>
  );
}
